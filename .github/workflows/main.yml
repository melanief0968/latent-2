name: Build and Deploy
on:
  push:
    branches:
      - main
env:
  VITE_APIKEY: AIzaSyDyAYYnPngX2knUnb_KYnXKbReHZW-by7A
  VITE_AUTHDOMAIN: latent-2022.firebaseapp.com
  VITE_DATABASEURL: https://latent-2022-default-rtdb.firebaseio.com
  VITE_PROJECTID: latent-2022
  VITE_STORAGEBUCKET: latent-2022.appspot.com
  VITE_MESSAGINGSENDERID: 527260667934
  VITE_APPID: 1:527260667934:web:d73363bed96200e3ac5e0f
  VITE_LOCATION_API_KEY: 8ccfb399b373416981422103bf030e78
  VITE_LOCATION_API_URL: https://api.opencagedata.com/geocode/v1/json
permissions: 
  contents: write
jobs:
  build-and-deploy:
    concurrency: ci-${{ github.ref }} # Recommended if you intend to make multiple deployments in quick succession.
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Install and Build 🔧 # This example project is built using npm and outputs the result to the 'build' folder. Replace with the commands required to build your project, or remove this step entirely if your site is pre-built.
        run: |
          npm ci
          npm run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4.3.3
        with:
          branch: gh-pages # The branch the action should deploy to.
          folder: dist # The folder the action should deploy.
